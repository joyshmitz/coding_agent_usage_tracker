[toolchain]
# Pinned, not floating: the clippy gate is `-D warnings`, so a rolling
# `nightly` turns every newly-added lint into a build failure on a tree nobody
# touched. This is the nightly the rest of the workspace is pinned to.
channel = "nightly-2026-08-25"
components = ["rustfmt", "clippy"]
